// Public business contact only. Never put API keys or secrets here.
window.PIZZA_CONFIG = {
  whatsapp: '', // International digits only, e.g. country code + number.
  contactEmail: '' // Your real business email for customization inquiries.
};
